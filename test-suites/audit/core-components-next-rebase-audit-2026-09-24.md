<!-- SPDX-FileCopyrightText: © 2026 Phala Network <dstack@phala.network> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->
# PR 841 next-rebase test audit (2026-09-24)

PR 841 was rebased from `e2cf39ae01` onto `next` at `0fb3b24bbd`. The 131 merged PRs in that range were reviewed by merged-PR boundary and mapped to acceptance regressions. Documentation-only, CI-only and SDK-only changes (#1244, #1264, #1266, #1304, #1316, #1335, #1359, #1360, #1370, #1389, #1392; SDK #1283, #1347, #1348) stay covered by the existing build, lint and SDK test gates. The `RawQuoteArgs` length contract documented by #1359 is recorded as a field constraint in `api-inventory.json`.

## Harnesses repaired by product changes

- #1282 removed OCI registry image pull. `tc-vmm-vmm-021` (`Vmm.ListRegistryImages`), `tc-vmm-vmm-022` (`Vmm.PullRegistryImage`) and `tc-vmm-compute-ne-006` (registry authentication, pull and extraction) and their mock registry fixture were retired. `tc-vmm-compute-ne-005` now asserts that the local-only image surface remains and the registry RPCs are gone.
- #1239 replaced the file-configured Gateway proxy certificate with `Admin.ImportCert`. The Gateway fixtures install the proxy certificate through the RPC (`tc-gw-internal-001`, `tc-gw-proxy-prot-003`), and the new `tc-gw-admin-037` covers the RPC itself.
- #1404 restricted certificate-named collateral fetches to allowlisted hosts. The simulator verifier and `dstack-util tpm-verify` configurations allow the local collateral host (`tc-gos-setup-022`, `tc-ver-input-plat-006`).
- #1332 removed the verifier debug path, #1251 moved the image manifest check in-process, and #1388 added download retries. The verifier cases follow the new request, response and error shapes (`tc-ver-cli-cert-o-003`, `tc-ver-cli-cert-o-004`, `tc-ver-input-plat-001`, `tc-ver-image-meas-001`).

## Focused regression boundaries

- VMM: #1230 disk preallocation (`tc-vmm-vmm-001`, `tc-vmm-configurat-001`, `tc-vmm-compute-ne-007`, `tc-vmm-vm-lifecyc-003`, `tc-vmm-ui-observa-005`); #1286 and #1351 port-mapping policy and all-or-nothing `UpdateVm` (`tc-vmm-compute-ne-002`, `tc-vmm-compute-ne-007`); #1346 offered-GPU listing and #1364 zero or overflowing resources (`tc-vmm-compute-ne-007`, `tc-vmm-vmm-001`); #1362 character-bounded compose preview (`tc-vmm-internal-006`); #1363 guest event name bound (`tc-vmm-configurat-001`); #1387 PCI hotplug off by default (`tc-vmm-configurat-001`, `tc-ver-tools-001`); #1390 and #1402 vmm-cli owner-only state and TLS verification (`tc-vmm-configurat-002`); #1398 pinned console dependencies (`tc-vmm-ui-observa-005`); #1227 and #1393 installer cleanup and `--locked` builds (`tc-vmm-install-007`). #1271 guest event persistence is covered by the existing lifecycle and observability cases.
- KMS: #1307 key handover on the admin listener (new `tc-kms-onboard-005`, `tc-kms-keys-certs-003`); #1305 and #1309 bootstrap over existing and partial root keys (`tc-kms-keys-certs-009`); #1310, #1311, #1312 and #1365 identity bounds, auth API bounds and timeouts, and `os_image_verification` visibility (`tc-kms-auth-006`, `tc-kms-auth-010`, `tc-kms-keys-certs-008`, `tc-kms-runtime-005`); #1314 single-block auth-eth decisions (`tc-kms-runtime-002`); #1344 empty device allowlist and #1391 unknown `MOCK_POLICY` (`tc-kms-auth-001`, `tc-kms-auth-002`); #1247 and #1343 SEV-SNP identity (`tc-kms-attestatio-002`); #1255 app certificates that authenticate as the KMS (`tc-kms-keys-certs-004`); #1302 and #1303 Nitro Enclave device id and release gate (`tc-kms-platform-006`, `tc-kms-release-010`); #1399 hourly boot info refresh (`tc-kms-auth-010`); #1349, #1396 and #1403 ct_monitor, pinned onboarding page and `--locked` images (`tc-kms-build-001`). #1228, #1313 and #1345 are covered by the existing auth and build cases.
- Gateway: #1245 relay stall, connection limit and zero-length writes (`tc-gw-internal-004`, `tc-gw-proxy-prot-003`, `tc-gw-proxy-prot-006`); #1278 and #1284 the 53-byte abort and SNI validation (`tc-gw-internal-004`, `tc-gw-proxy-prot-001`, `tc-gw-proxy-prot-003`, `tc-kms-auth-010`); #1241 certbot key handling and #1262 ACME off the startup path (`tc-gw-certbot-001`, `tc-gw-certbot-003`, `tc-gw-certbot-006`, `tc-gw-certificat-001`); #1355, #1356 and #1357 token redaction, domain normalization and atomic locks (`tc-gw-certificat-002`, `tc-gw-certificat-003`, `tc-gw-certificat-007`, `tc-gw-admin-027`, `tc-gw-admin-028`, `tc-gw-kv-009`); #1276, #1374 and #1380 WaveKV sync (`tc-gw-cluster-ad-002`, `tc-gw-kv-009`); #1253, #1265, #1385 and #1386 routing-lock regressions (`tc-gw-cluster-ad-004`); #1308 WireGuard apply batching (`tc-gw-registrati-002`, `tc-gw-cluster-ad-004`); #1400 `public_tcbinfo` on the public app-info route (`tc-gw-internal-005`). #1381 is covered by `tc-gw-proxy-prot-004`, and #1375 (removed `external_port` and `tappd_port`) by the cluster cases and the configuration inventory.
- RPC framework: #1234 bounded, representation-matched error text and #1401 log-line forging in the request-contract matrix (`tc-gos-contract-001`..`004`, `tc-kms-contract-001`..`002`, `tc-gw-contract-001`..`003`).
- Guest agent and dstack-util: #1232 certificate validity bounds (`tc-gos-dstackguest-001`); #1263 anonymous `Info` (`tc-gos-worker-001`); #1256 quote-driven SIGABRT (`tc-gos-concurrency-002`); #1233 owner-only boot secrets and #1341 env delivery (`tc-gos-boot-and-i-003`, `tc-gos-platform-006`); #1243 env-file escaping (`tc-gos-setup-001`); #1254 compose `include:` and the host-share mount (`tc-gos-setup-003`, `tc-gos-platform-003`); #1320 and #1326 os-image-hash and storage-encrypted events (`tc-gos-boot-and-i-001`); #1323 omitted `instance_id` (`tc-gos-setup-005`); #1339 interrupted data-disk initialization (`tc-gos-setup-007`); #1240 and #1358 supervisor semantics and log reopening (`tc-gos-setup-012`, `tc-gos-storage-an-004`); #1318 and #1353 GPU claim event and clock sync (`tc-gos-gpupolicy-007`). #1242, #1280, #1317, #1340, #1371, #1372, #1373, #1394 and #1397 are covered by the existing setup, storage, platform and observability cases.
- Guest image: #1274 initramfs mounts and verity policy, #1321 terminal presets, #1327 sysctl, #1329 NVIDIA runtime log level and #1331 build-time rootfs tmpfiles (`tc-gos-platform-005`); #1322, #1324 and #1328 unit ordering and the boot-error unit (`tc-gos-platform-006`); #1325 journald budget (`tc-gos-platform-007`); #1354 NTS-only chrony (`tc-gos-yocto-003`). #1330 is covered by the existing boot and platform cases.
- Measurement and verifier: #1229 canonical setup header, #1231 and #1236 PE and TDVF parser bounds, and #1367 `dstack-mr diagnose` (`tc-ver-tools-001`, `tc-ver-tools-002`); #1238, #1267, #1275, #1338 and #1404 collateral fetch bounds, PCR validation and the host allowlist (`tc-ver-input-plat-005`, `tc-ver-input-plat-006`, `tc-ver-build-002`, `tc-gos-setup-022`); #1248, #1252 and #1279 SEV-SNP input bounds (`tc-ver-input-plat-005`, `tc-ver-tools-003`); #1251, #1334, #1337 and #1369 image manifest, cache key and cache bounds (`tc-ver-image-meas-001`, `tc-ver-image-meas-005`, `tc-ver-cli-cert-o-003`, `tc-int-compatibil-005`); #1333 `--verify-cert` validity (`tc-ver-cli-cert-o-002`); #1388 download retries (`tc-ver-build-002`). #1285 and #1336 are covered by `tc-ver-tools-003` and the image measurement cases.

## Catalog maintenance

- `api-inventory.json` was regenerated from the protobuf sources while preserving hand-written field constraints. It drops `Vmm.ListRegistryImages`, `Vmm.PullRegistryImage` and their messages, adds `VmConfiguration.disk_prealloc`, KMS `Admin.GetKmsKey` (`tc-kms-onboard-005`), `OnboardRequest.source_token`, `GetMetaResponse.os_image_verification` and Gateway `Admin.ImportCert` (`tc-gw-admin-037`), and records the `RawQuoteArgs.report_data` length contract from #1359.
- `configuration-inventory.json` drops `image.registry` and `core.proxy.external_port`, and adds `max_event_name_len`, `cvm.disk_prealloc`, `core.nitro_enclave_key_release`, `core.admin.tls.key`, `core.admin.tls.certs`, `core.auth_api.webhook.timeout`, `core.onboard.public_key_handover` and `allowed_collateral_hosts` for the KMS, Gateway and verifier. It also picks up example keys that were missing before this range (`cvm.shuffle_*_urls`, `core.attest_rpc_cert`, the Gateway health-check, thread-per-core and WaveKV keys) and drops keys the example files no longer carry. The source-only Gateway fields `base_domain`, `cert_chain` and `cert_key` are gone.
- `source-inventory.json` and `source-coverage-map.json` add the new product files (`pki-fetch`, `sha256sum.rs`, `wg_apply.rs`, `dstack-boot-error@.service`, `os/mkosi/rootfs.tmpfiles`), follow the `dstack-image.conf` rename, drop `registry.rs` and `dstack-image-oci.sh`, and map files that the range touched but that had no coverage entry.
- `tc-vmm-vmm-021`, `tc-vmm-vmm-022` and `tc-vmm-compute-ne-006` were removed from the feature audit, the coverage map and the promoted list. `tc-kms-onboard-005` and `tc-gw-admin-037` are not promoted until they pass on hardware.

## Product findings

- #1331: `mkosi.finalize` runs on the host as the invoking user in a rootless build, so `systemd-tmpfiles` cannot `chown` the rootfs directories to root, fails with `EINVAL` and aborts every rootless image build. Fixed by #1409, which runs it through `mkosi-chroot`.
- #1387: the `dstack-mr` CLI still defaults `--hotplug-off` to false, so it measures a different RTMR0 than a VMM running with the new default. Fix: PR #1405.
- #1307: onboarding with `source_token` still calls `GetTempCaCert`, which the admin listener does not serve, and fails with `Service not found: GetTempCaCert`. `tc-kms-onboard-005` depends on the fix, PR #1406.
- #1401: a percent-encoded line break in the method segment can still forge a log line through the request span's method field. The L7 check in `tc-gw-contract-001`..`003` looks for this and is expected to fail on `next` until the fix merges. Fix: PR #1407.
- #1308: the detached WireGuard apply thread can be cut off by process exit between writing and renaming its temporary config, leaving `.wireguard.conf.*.tmp` with the interface private key behind; `tc-gw-internal-001` caught it intermittently. Fix: PR #1408.
- #1331: `/var/lib` is a writable overlay, so without a boot-time entry `systemd-tmpfiles-setup` re-applies `tpm2-tss-fapi.conf` and the TPM keystore reverts to `2775 tss:tss` at runtime (`tc-gos-platform-005`). Fix: PR #1413.
- #789: the `gpu-attestation` runtime event carries a per-boot evidence digest and is extended into PCR14 before the TPM key provider unseals, so a GPU CVM on GCP or AWS with `key_provider = tpm` cannot unseal after a reboot. Found on a GCP a3-highgpu-1g H100 run of the GPU cases. Fix: PR #1410.
- `systemd-tpm2-setup` fails on the read-only root and leaves the guest degraded. Fix: PR #1411.
- 0828cb45df: the pooled Unix-socket HTTP client outlives the supervisor's 15s keep-alive, so a VMM request can fail with `EPIPE` (`tc-kms-upgrade-010`). Fix: PR #1412.
- `Vmm.StopVm` returns before QEMU exits and a `StartVm` in that window is silently skipped (`tc-gos-setup-008`). Fix: PR #1414.
