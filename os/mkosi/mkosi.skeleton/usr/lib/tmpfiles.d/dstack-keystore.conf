# SPDX-License-Identifier: Apache-2.0
# /var/lib is a writable overlay, so tpm2-tss-fapi.conf re-applies its 2775
# tss:tss keystore on every boot. This file sorts first and keeps the image's
# 0755 root:root (rootfs.tmpfiles) at runtime too.
d /var/lib/tpm2-tss/system/keystore 0755 root root -
